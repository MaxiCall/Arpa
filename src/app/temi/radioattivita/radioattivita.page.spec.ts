import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RadioattivitaPage } from './radioattivita.page';

describe('RadioattivitaPage', () => {
  let component: RadioattivitaPage;
  let fixture: ComponentFixture<RadioattivitaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RadioattivitaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
